/ {
    aliases {
        custom-led = &my_external_led;
    };

    leds {
        compatible = "gpio-leds";
        my_external_led: led_d7 {
            /* P1.12 maps to &gpio1 12 */
            gpios = <&gpio1 12 GPIO_ACTIVE_HIGH>;
            label = "External LED on D7";
        };
    };
};